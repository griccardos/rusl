[package]
name = "rusl"
version = "0.6.0"
edition = "2024"
build = "build.rs"

[dependencies]
librusl = { path = "../librusl" }
rfd = "0.15"                      #for selecting directory slint = 
slint = "1.13"                    #GUI

[build-dependencies]
slint-build = "1.13" #slint GUI
